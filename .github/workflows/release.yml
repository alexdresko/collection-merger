name: Deprecated Release (use release-please.yml)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Releases are handled by .github/workflows/release-please.yml now."
